# 部署与邮件状态

截至本次交付：

- 已创建完整 Python 项目、配置、GitHub Actions 工作流、中文 README 和自动化测试。
- 已在本机真实访问学校网站；逐学院结果见验收报告。
- 用户已提供目标仓库 https://github.com/muluzzh00-cpu/pre-recommend-monitor ，本地已设置远端并提交代码。上传当前等待 GitHub 设备登录授权完成；尚未确认远端部署、云端 schedule 或云端运行。
- 新增 `Project tests` 自动化测试工作流和 `Cloud crawl acceptance` 两轮真实抓取验收工作流。后者使用独立测试状态和模拟邮件，不需要邮箱密码，不影响生产通知去重。
- 用户选择稍后配置发送邮箱，当前不存在 `.env`。没有向 Gmail 发送真实邮件。邮件 HTML、SMTP 协议调用、失败状态与提醒逻辑已测试；真实 SMTP 发信及收件验收仍待配置。
- 本机测试状态与部署后的生产状态相互隔离；模拟通知不视为生产已发送。

现在不能将本系统称为“已部署、关机后正在监控”。完成 README 中上传、Secrets、测试邮件、两轮云端运行和两个 schedule 记录后，才能确认启用。

如有 GitHub CLI，可先在自己的终端执行 `gh auth login`，然后在项目目录执行：

```powershell
./scripts/deploy.ps1 -Repository YOUR_ACCOUNT/pre-recommend-monitor
```

该脚本用于创建私有仓库；当前用户已提供仓库，后续直接推送到该仓库。不要在聊天中提供密码或令牌。随后在 GitHub 网页输入五项 Secrets。
